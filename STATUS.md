# Nebula Code 実装状況

このファイルは各機能の到達点を正直に記録するもの。「動く」と書いてあるものは
実際に動かして確かめた。確かめていないものはそう書いてある。

計測環境: macOS 26 (Darwin 25.5.0) / Apple Silicon / Rust 1.97.1 / gpui 0.2.2

---

## アーキテクチャ & 非機能要件

| 要件 | 状態 | 実測・根拠 |
|---|---|---|
| 冷間起動 200ms 以下 | **達成** | release ビルド、ページキャッシュ温、`main`→初回描画 **中央 113ms** (87.8–119.8ms、6 回計測)、`exec`→初回描画 **中央 131ms** (105.8–139.8ms)。`NEBULA_TRACE_STARTUP=1` で再現可能 |
| マルチプロセス (GUI / 状態管理) | **達成** | Unix ドメインソケット + 長さ prefix + MessagePack。結合テスト 7 件で通し確認 |
| Cyber-Cosmic テーマ | **達成** | 深宇宙の暗色 + シアン/マゼンタ/バイオレットの 3 アクセント。24 種のトークン配色、ANSI 256 色、埋め込み SVG アイコン 26 種 |

**起動時間の計測方法について正直に書くと**: 上記はどちらもページキャッシュが温まった
状態の値。バイナリを書き直した直後 (キャッシュ冷) では 186–243ms を観測している。
「冷間起動」を「OS 再起動直後の初回実行」と定義するなら未計測。日常的な起動
(2 回目以降) では確実に 200ms を切る。

計測は `exec` から「最初のフレームを描き終えた瞬間」まで。ウィンドウ生成の関数が
返った時点ではなく、実際に描画が終わった時点を測っている (前者は描画前に返るため)。

---

## 機能

### エディタコア — **動く**
- ropey ベースの Rope バッファ、Undo/Redo (トランザクション単位)、マルチカーソル
- Tree-sitter による構文解析 11 言語 (Rust / TypeScript / TSX / JavaScript / Python /
  Go / JSON / TOML / Markdown / HTML / CSS)
- **解析はバックエンドで実行**し、可視行のハイライトスパンだけを GUI へ送る
- 書記素クラスタ単位のカーソル移動、UTF-16 との相互変換 (IME 用)
- 括弧対応の判定は `nebula-core` に集約し GUI とバックエンドで共有

### エディタビュー — **動く**
- GPUI のカスタム `Element` として実装。**可視行だけをシェイプして描く**ので
  行数に比例したコストがかからない
- カーソル / 選択 / 複数カーソル / 現在行 / インデントガイド / 行番号
- git 差分ガター、診断の波線、対応括弧の下線
- IME (未確定文字の下線、変換候補ウィンドウの位置)
- 補完ポップアップ (絞り込み・Tab/Enter 確定・スニペットのプレースホルダ展開)
- ホバーカード
- タブ、2 分割ペイン、名前を付けて保存、新規ファイル

**確かめていないこと**: 実ウィンドウでの目視確認。この環境では画面キャプチャの
権限が無く、GUI を人手で操作することもできなかった。プロセスが落ちないこと、
バックエンドと通信できていることは確認済みだが、**見た目と操作感は未検証**。

### エクスプローラー — **動く**
- 遅延展開のツリー、`uniform_list` による仮想化、gitignore の淡色表示
- 新規作成 / 名前の変更 / 複製 / 削除 / パスのコピー、右クリックメニュー
- ファイル監視による自動追従

入力欄を開いたときは `UniformListScrollHandle` でその行を画面中央へ送る。
`uniform_list` は可視範囲しか要素を作らないため、これをやらないと入力欄の行が
画面外にあるとき打鍵が一切入らない。

### 検索 (ripgrep 統合) — **動く**
- `rg --json` のストリーミング解析。結果は逐次届く
- ファイル単位のグループ表示、一致部分の強調、正規表現/大小区別/単語単位
- include/exclude glob、一括置換
- ファイルのあいまい検索 (クイックオープン)

実測確認: 日本語を含む検索で文字オフセットがずれないこと、2 ファイル 2 件の
検出、完了イベントの到達を結合テストで確認。

### Git 統合 — **動く**
- status (porcelain v2) / branches / blame / stage / unstage / discard /
  commit / checkout / log / push / pull
- 行ガターの差分は **常に HEAD と比較**する。ステージしても印が消えない
  (自前の Myers 行差分。未保存の内容に対しても差分が取れる)

実測確認: 一時リポジトリに対して init → commit → 変更 → stage → commit → log までを
結合テストで通した。`DiffHunk.new_start` が 0 始まりであることも検査済み。

### LSP 連携 — **動く**
- Content-Length フレーミングの JSON-RPC クライアント、UTF-16 位置変換
- hover / completion / definition / references / documentSymbol / formatting /
  rename / signatureHelp / codeAction
- rust-analyzer / typescript-language-server / pyright-langserver の起動設定
- 診断は `publishDiagnostics` を受けて GUI へ流す

**rust-analyzer で実測確認済み**: このリポジトリ自身に対してホバー (ropey の
ドキュメント)、定義ジャンプ (ropey のソースへ)、ドキュメントシンボル 8 件、
補完候補 96 件が返ることを確認 (`cargo test -p nebula-backend --test lsp_rust_analyzer -- --ignored`)。

**未検証**: typescript-language-server と pyright はこの環境に入っていないため
起動確認ができていない。Bun は「Node と同じ tsserver を使う」以上の専用対応はしていない。

### ターミナル — **動く**
- portable-pty + vte。セルグリッドと SGR (16/256/RGB 色、太字・斜体・下線・反転)
- 差分更新 (変化した行だけ、60Hz に間引き)、全角文字、スクロールバック
- **代替画面バッファ** (`?1049` / `?47` / `?1047`) — vim / less / htop が使える。
  代替画面の内容はスクロールバックに積まない
- **DECCKM** — アプリケーションカーソルキーモード。`TerminalUpdate` を変更できないため、
  モードを知っているバックエンド側で `ESC [ A` → `ESC O A` を書き換える方式で解決
- **端末クエリへの応答** (`CSI 6n` / `CSI 5n` / `CSI c`) — 位置問い合わせで固まらない
- **DEC 罫線文字集合** (`ESC ( 0`) — `tree` 等の枠線が正しく出る
- **背景色つき消去 (BCE)**、DECSC/DECRC、カーソル退避・復元
- GUI 側はカスタム `Element` で 1 行 1 シェイプ
- 実測確認: `/bin/sh -c "printf NEBULA_OK"` の出力と終了イベント、
  グリッドレベルの ANSI 解釈 43 件のテスト

**未対応**: マウス報告、bracketed paste、SO/SI と G1 指定、
二次 DA (`CSI > c`)、スクロールで入る新規行への BCE

### Codex 連携 — **部分的**
- `codex app-server` の JSON-RPC クライアント (生成済み JSON Schema に基づく実装)
- 会話 UI、ストリーミング表示、推論の折り畳み、コマンド実行ブロック、
  承認フロー (許可 / セッション中は常に許可 / 拒否 / 中止)
- 実測確認: 認証状態の問い合わせが応答することを確認

**未検証**: 実際に会話を成立させるにはログインが要り、この環境では行っていない。
イベント翻訳は単体テストで検査しているが、**実サーバーからのイベントで確かめていない**。

### タブ・ペイン・ワークスペース管理 — **動く**
- タブバー (未保存印、分割ボタン)、2 分割ペイン
- Slack 風のワークスペース切り替え (アクティビティバー上段)
- サイドバー / 下部パネルのドラッグリサイズ
- セッション永続化 (フォルダ・パネル寸法・表示状態)。**開いていたタブは復元しない**

### コマンドパレット・問題パネル — **動く**
- `>` の有無でコマンド / ファイル検索を切り替え、ファジー絞り込み
- 診断一覧 (重要度順、ファイルごとのグループ、絞り込み)

---

## 品質

- **テスト 491 件** (`cargo test --workspace`)。全通過、失敗 0、無視 2 (外部プロセス依存)
  - nebula-protocol 5 / nebula-core 48 / nebula-backend 248 + 結合 12 / nebula 178
- 警告 4 件 (いずれも未使用のデッドコード)
- 外部コマンドの出力パーサはすべて純粋関数に切り出し、実際に採取した出力で検査

### 入力欄の共通化

テキスト入力の実装が 6 ビューに複製されており、**転記ずれによる IME バグ 3 件の
発生源**になっていた。`ui::TextInput` に集約し、`impl EntityInputHandler` を
`ui.rs` とエディタ本体の 2 か所だけにした。

- 6 ファイル合計 **12,303 → 10,299 行** (-2,004)。共通部品にはテストが付いている
- 単一行 / 複数行、呼び出し側によるキー予約 (パレットの上下キー、codex の Enter) に対応
- 3 バグそれぞれに回帰テストを置き、**意図的にバグを注入して赤になることを確認済み**

### 開発中に見つけて直した実バグ

自分で書いた箇所:
1. Undo の逆編集を適用順と逆にたどっていなかった (複数編集を含む Undo が壊れる)
2. ハイライトの行範囲で終端を包含として扱っていた (範囲外に色がつく)
3. 保存が編集キューを追い越し、ディスクに古い内容が書かれる競合
4. ロックファイル方式のバックエンド排他が競合し、GUI 3 つで **8 プロセス**に増殖
   (`bind` の原子性に置き換えて解決。実機で 3 起動 → 1 プロセスを確認)
5. macOS の `bind` は `EEXIST` を返す (`EADDRINUSE` だけ見ていた)
6. `Session::default()` と空 JSON の読み込みで初期状態が食い違う
7. セッションを `~/.config` に書こうとしていた。macOS の作法は
   `~/Library/Application Support`。しかもこの環境の `~/.config` は書き込み権限が無く
   (`d--x--x--x`)、**保存が黙って失敗していた**。稼働中のバックエンドへ外から
   `ListWorkspaces` を投げる診断テストで切り分けた

検証エージェントが見つけた箇所:
8. 検索の一括置換で、NUL を含むファイルが **27% 切り詰められる** データ破壊
9. 区切りを含む glob (`target/**`) が黙って無効
10. ファイル監視で、ルートが無視名フォルダ配下にあると全イベントが消える
11. 同時に走った 2 つのアトミック保存が偽のリネームイベントを生む
12. IME の選択範囲がバッファ外に出てパニックしうる (3 箇所の転記ずれ)

---

## 積み残し

| 項目 | 影響 |
|---|---|
| 実ウィンドウでの目視確認 | 見た目と操作感がすべて未検証 |
| ネストした `.gitignore` | ルート直下のみ反映 |
| Git: 引用符を含むファイル名 | `-z` を使っていないためパスが壊れる |
| Git: コミット 0 件のリポジトリ | log と HEAD 参照がエラーになる |
| 設定画面 | 未着手。テーマ・キーバインドの変更は再ビルドが必要 |
| 検索の隠しファイル | `--hidden` を渡す経路がない |
| 同一語のハイライト | テーマに色はあるが未実装 (`selection_match`) |

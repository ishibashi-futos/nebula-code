# Nebula Code アーキテクチャ

## 全体像

Nebula Code は 2 つのプロセスで動く。

```
┌──────────────────────────┐         ┌─────────────────────────────┐
│  nebula (GUI)            │         │  nebula-backend             │
│  ─────────────────────   │  Unix   │  ────────────────────────   │
│  gpui / Metal 描画       │ socket  │  tokio / 外部プロセス管理    │
│  入力処理・レイアウト     │ ◄─────► │  バッファ正本・LSP・PTY      │
│  バッファの描画用複製     │ msgpack │  git・ripgrep・Codex         │
└──────────────────────────┘         └─────────────────────────────┘
```

分離の狙いは 2 つ。

1. **描画スレッドを外部要因から守る。** 言語サーバーの初期化、ripgrep の全文検索、
   git のサブプロセス起動はいずれも数百 ms 単位で待たされることがある。同一プロセスに
   置くと、非同期にしていてもメモリ確保やロックを通じて描画に影響が出る。
2. **GUI を軽く保つ。** 構文解析 (Tree-sitter)、検索インデックス、PTY のスクロールバックは
   すべてバックエンド側に置く。GUI が持つのは可視領域を描くために必要な情報だけ。

バックエンドはユーザーあたり 1 つ。複数ウィンドウは同じバックエンドに接続し、
ワークスペースやバッファの状態を共有する。

## クレート構成

| クレート | 役割 |
|---|---|
| `nebula-protocol` | IPC のメッセージ定義とフレーミング。両プロセスが依存する唯一の共通型。 |
| `nebula-core` | Rope バッファ、カーソル操作、Tree-sitter 構文解析。UI にもトランスポートにも依存しない。 |
| `nebula-backend` | 状態管理プロセス。バイナリとライブラリの両方を提供する。 |
| `nebula` | GUI プロセス。gpui のビューとテーマ。 |

`nebula-core` を両プロセスから使うのは、GUI 側も編集を即時に反映するために
Rope を持つため。ただし **構文解析は GUI では走らせない**。ハイライトは
バックエンドが計算し、可視行範囲ぶんのスパン列だけを送る。

## バッファの正本と複製

編集の流れ:

1. GUI がキー入力を受け取り、自分の複製に **楽観的に** 適用して即座に再描画する。
2. 同じ編集を `ApplyEdits { base_version, edits }` でバックエンドへ送る。
3. バックエンドが正本へ適用し、構文木をインクリメンタル更新し、LSP に `didChange` を送る。
4. 新しい版数が返る。GUI は自分の版数と突き合わせる。

`base_version` が食い違った場合はバックエンドが拒否する。GUI は正本を取り直す。
これが起きるのは、外部からのファイル変更を取り込んだ直後など限られた場面だけ。

打鍵ごとに IPC の往復を待って描画すると、体感がネットワーク越しのエディタになる。
そのため **描画は待たない**。ハイライトだけが 1〜2 フレーム遅れて追いつく。

## IPC

- **トランスポート**: Unix ドメインソケット (`$XDG_RUNTIME_DIR` または `$TMPDIR` の
  `nebula-<版数>.sock`)
- **フレーミング**: 4 バイトのビッグエンディアン長 + MessagePack ペイロード
- **メッセージ**: `Request` / `Response` / `Event` の 3 種のみ

MessagePack を選んだのは、ターミナル出力やハイライトスパンのような高頻度メッセージで
JSON よりバイト数と直列化コストの双方が小さいため。

ストリーミングする結果 (検索、ターミナル出力、Codex 応答) は、開始要求に対して即座に
ID を返し、以降の増分を `Event` で流す。GUI は結果全体を待たずに描き始められる。

GUI 側の I/O は専用の OS スレッド 2 本 (読み・書き) で回す。gpui の executor 上で
ブロッキング I/O を回すと描画タスクの実行枠を奪うため、物理的に分けている。

### バックエンドを 1 つに保つ方法

排他は `UnixListener::bind` そのものに任せる。bind はソケットファイルの作成と
待ち受け開始を 1 回のシステムコールで行うので、複数プロセスが同時に呼んでも
成功するのは 1 つだけ。敗者は静かに終了し、GUI 側の再接続ループが勝者に繋がる。

別途ロックファイルを置く方式は **採らない**。「ロックはあるが接続できない = 残骸」
という判定が、勝者が bind する直前の一瞬にも成立してしまい、敗者がロックを奪って
二重起動する。実際に GUI を 3 つ同時起動して 8 プロセスに増えるのを観測した。

異常終了でソケットファイルだけが残った場合は、接続できないことを 2 回確かめてから
片付ける。1 回で判断しないのは、bind と listen の間のごく短い時間に接続が
拒否されうるため。

### 編集要求の順序

`ApplyEdits` は **1 件ずつ順に** 送る。バックエンドは要求ごとに別タスクで処理するので、
並行して送ると適用順が入れ替わり版数が食い違う。保存 (`SaveBuffer`) も同じ待ち行列に
載せる。独立して投げると、まだ送っていない編集を追い越してディスクに古い内容が書かれる。

## 冷間起動 200ms

`main` から最初のフレームまでに置くのは以下だけ。

- テーマとキーバインドの登録 (定数の構築のみ)
- ウィンドウ生成

以下はすべてフレーム後に回す。

- バックエンドの起動と接続 (`cx.spawn` + バックグラウンド実行)
- Tree-sitter のハイライトクエリのコンパイル (言語ごとに、そのファイルを開いた時点で)
- git 状態の取得、ファイル監視の開始
- 言語サーバーの起動

Metal シェーダはビルド時にプリコンパイルする (`runtime_shaders` フィーチャは使わない)。
起動時のシェーダコンパイルは数十 ms かかる。

計測は `NEBULA_TRACE_STARTUP=1` を付けて起動すると標準エラーに出る。

## 外部ツールへの依存

いずれも「あれば使う」。無くても起動はでき、該当機能だけが無効になる。

| 用途 | 使うもの | 無い場合 |
|---|---|---|
| 全文検索 | `rg --json` | 検索ビューが案内を出す |
| バージョン管理 | `git` CLI (porcelain 形式) | Git ビューが無効 |
| 言語支援 | `rust-analyzer` / `typescript-language-server` / `pyright-langserver` | 構文着色のみ |
| AI 連携 | `codex app-server` | Codex ビューが案内を出す |

自前でライブラリを抱え込まず CLI を叩くのは、(1) バイナリサイズとビルド時間を抑えられ、
(2) ユーザーが普段使っているものと同じ挙動になるため。ripgrep と git はどちらも
機械可読な出力形式を持っているので、解析は安定する。

## ターミナル

`portable-pty` で PTY を起動し、`vte` で ANSI シーケンスを解釈してセルのグリッドを
バックエンドが保持する。GUI へ送るのは **変化した行だけ**。全画面を毎フレーム送ると
`rows × cols` 個のセルが IPC を流れて重い。

`libghostty` ではなく `portable-pty` + `vte` を使うのは、前者が Ghostty 本体の
ソースからのビルドを必要とし、Rust クレートとして安定供給されていないため。

## セッション

前回開いていたフォルダ、サイドバーの幅と表示状態、パネルの高さを
`$XDG_CONFIG_HOME/nebula/session.json` (無ければ `~/.config/nebula/session.json`) に持つ。

**開いていたファイルは復元しない。** 復元するとバッファを開く要求が起動直後に何本も走り、
最初のフレームの後とはいえ体感の立ち上がりが鈍る。VS Code と同じく
「フォルダは覚える、タブは覚えない」を既定とする。

## テスト方針

- `nebula-core` と `nebula-protocol` は純粋なロジックなので単体テストで固める。
- バックエンドの各サービスは、外部コマンドの出力パーサを独立した関数に切り出して
  テストする。実際のプロセス起動を伴う経路は一時リポジトリ・一時ディレクトリを作る
  結合テストで確認する。
- GUI は gpui の描画に依存する部分が多いため、状態遷移とデータ整形だけを切り出して
  テストする。

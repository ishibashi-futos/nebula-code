* [ ] 開いたファイル「✖️」ボタンでが閉じれない（コマンドパレットの「タブを閉じる」からは閉じれる
* [ ] コマンドパレットの表示を「英語名: 日本語名」のように表示できるようにする。例： Terminal: ターミナルを開く, Toggle Sidebar: サイドバーの表示切り替え
* [ ] ターミナルを開いたとき、デフォルトでデフォルトシェルを起動できるようにする（ターミナルを起動を出さない）
* [ ] ワークスペースの追加ボタンを押してもワークスペースが追加できない
* [ ] UI上の各種ボタンにTooltipを追加する
* [ ] ターミナル上のコピー・ペースト機能を追加（右クリックでコンテキストメニュー表示なども）
* [ ] `nebura update` によるSelf Update機構の追加(reference: `bake update` https://github.com/ishibashi-futos/overbake)
* [ ] `.git`, `.gitignore` 等の隠しフォルダ非表示、再表示機能
* [ ] `JetBrains Mono` の組み込み・デフォルト化
* [ ] Markdownファイル記述支援機構の追加（箇条書き、チェックリスト改行時自動作成、見出しのハイライトなど）
* [ ] Markdownファイルプレビュー機能の追加
* [ ] gitの差分有無がエクスプローラーからも確認できて欲しい
* [x] エディタで最終行にカーソルがある場合に、↓キーを押すと、最終行の末尾列までカーソルが移動して欲しい
* [ ] 古いバックエンドプロセスが生き残っていると、新しい GUI がそちらに相乗りして古いコードで動き続ける
      （ソケット名が `nebula-{PROTOCOL_VERSION}.sock` 固定で、`connect_or_spawn` が既存ソケットへ無条件に接続するため。
      「直したのに直らない」が再発する）。起動時ハンドシェイクでバージョン／ビルド ID を突き合わせ、
      不一致なら古いデーモンを終了させてから起動し直す（あるいは `--replace` 相当を用意する）
* [ ] GUI プロセスが子プロセスを回収しない（SIGCHLD をブロック／無視したままにしている）。
      バックエンド側は起動時にシグナル状態を戻すようにしたが、GUI 自身は終了した
      nebula-backend をゾンビとして残す。GUI 側でも同じ初期化を行うか、子の回収を行う
* [ ] バックエンドの待ち受け開始が `tools::detect_all()` の完了に依存している。
      検出に時間がかかるツールがあると、その間 GUI から接続できない
      （現在は 1 ツール 2 秒で打ち切ることで最悪 2 秒に抑えている）。
      本来は先にソケットを開き、ツール検出は起動後に非同期で行って結果をイベントで通知したい
